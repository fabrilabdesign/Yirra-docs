apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n-mcp-server
  labels:
    app.kubernetes.io/name: n8n-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: n8n-mcp-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: n8n-mcp-server
    spec:
      containers:
        - name: server
          image: localhost:5000/mcp/mcp-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - { name: PORT, value: "8080" }
            - { name: MCP_KIND, value: "n8n" }
            - { name: N8N_BASE_URL, value: "http://n8n.tools.svc.cluster.local:5678" }
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"


